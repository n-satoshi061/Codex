# うちの在庫ノート

家庭向けの在庫管理アプリです。食品、飲料、日用品などを登録し、残量不足と期限が近い在庫を一覧できます。

## セットアップ

```bash
npm install
npm run dev
```

## 主な機能

- カテゴリ、保管場所、数量、単位、期限、メモ付きで在庫を登録
- 検索とカテゴリフィルタで在庫を絞り込み
- 残量の増減をワンクリックで更新
- 下限以下の在庫を買い物メモに自動表示
- `localStorage` に保存してブラウザ再読み込み後も保持
